@using JailTalk.Shared.Utilities;
@inject NavigationManager NavigationManager

<MudCard Elevation="1" Outlined=true @onclick="@NavigateToRoute" Class="db">
    <MudCardContent>
        <MudPaper Elevation="0" Width="100px" Height="100px"
                  Square=true
                  Class="d-flex align-center justify-center shades transparent">
            <MudText Typo=Typo.h6 Align="Align.Center">@Name</MudText>
        </MudPaper>
    </MudCardContent>
</MudCard>
@code {

    [Parameter]
    public string Name { get; set; }

    [Parameter]
    public string Route { get; set; }

    public void NavigateToRoute()
    {
        if (string.IsNullOrEmpty(Route))
        {
            throw new AppException("Route cannot be empty");
        }

        NavigationManager.NavigateTo(Route);
    }
}
